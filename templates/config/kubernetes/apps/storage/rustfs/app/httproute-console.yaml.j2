#% if rustfs_enabled | default(false) %#
---
#| =========================================================================== #|
#| HTTPRoute for RustFS Console UI via envoy-internal (local network access)   #|
#| =========================================================================== #|
#| Exposes RustFS management console for:                                       #|
#|   - Creating buckets                                                         #|
#|   - Creating access keys for Loki/Tempo/other services                       #|
#|   - Monitoring storage usage                                                 #|
#|   - Managing IAM policies (via UI, not mc admin commands)                    #|
#|                                                                              #|
#| Access: https://rustfs.{domain} (internal network only)                     #|
#| =========================================================================== #|
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: rustfs-console
spec:
  hostnames:
    - "rustfs.${SECRET_DOMAIN}"
  parentRefs:
    - name: envoy-internal
      namespace: network
      sectionName: https
  rules:
    - backendRefs:
        #| RustFS Helm chart creates service named 'rustfs-svc' #|
        - name: rustfs-svc
          port: 9001
      matches:
        - path:
            type: PathPrefix
            value: /
#% endif %#
