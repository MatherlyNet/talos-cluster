#% if monitoring_enabled | default(false) and loki_enabled | default(false) %#
---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./ocirepository.yaml
  - ./helmrelease.yaml
#% if rustfs_enabled | default(false) %#
  - ./secret-s3.sops.yaml
#% endif %#
#% if loki_monitoring_enabled | default(false) %#
  - ./dashboard-stack-monitoring.yaml
#% endif %#
#% endif %#