#% if litellm_enabled | default(false) or langfuse_enabled | default(false) or obot_enabled | default(false) %#
---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: ai-system

components:
  - ../../components/sops

resources:
  - ./namespace.yaml
#% if litellm_enabled | default(false) %#
  - ./litellm/ks.yaml
#% endif %#
#% if langfuse_enabled | default(false) %#
  - ./langfuse/ks.yaml
#% endif %#
#% if obot_enabled | default(false) %#
  - ./obot/ks.yaml
#% endif %#
#% endif %#
