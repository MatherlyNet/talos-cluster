# OpenTofu state files (should never be committed)
*.tfstate
*.tfstate.*
*.tfplan

# Local .terraform directories
**/.terraform/

# Crash logs
crash.log
crash.*.log

# NOTE: secrets.sops.yaml IS committed (it's encrypted)
# The age.key file in repo root is in the main .gitignore