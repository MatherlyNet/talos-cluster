---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: obot
  namespace: ai-system
  labels:
    app.kubernetes.io/name: obot
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: obot
  namespaceSelector:
    matchNames:
      - ai-system
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
      scrapeTimeout: 10s
