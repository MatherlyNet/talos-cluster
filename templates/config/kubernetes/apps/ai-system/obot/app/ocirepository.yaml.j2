#% if obot_enabled | default(false) %#
---
#| ============================================================================= #|
#| OBOT OCI REPOSITORY - Custom Helm chart from jrmatherly/obot-entraid         #|
#| NOTE: Chart version uses NO 'v' prefix (e.g., 0.2.32)                         #|
#| NOTE: Image tag uses 'v' prefix (e.g., v0.2.32) - set in helmrelease.yaml.j2 #|
#| REF: https://github.com/jrmatherly/obot-entraid                               #|
#| REF: docs/research/obot-mcp-gateway-integration-jan-2026.md                   #|
#| ============================================================================= #|
apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository
metadata:
  name: obot
  namespace: ai-system
spec:
  interval: 15m
  layerSelector:
    mediaType: application/vnd.cncf.helm.chart.content.v1.tar+gzip
    operation: copy
  ref:
    #| Chart version (no 'v' prefix) - should match obot_version for consistency #|
    tag: #{ obot_chart_version | default(obot_version) | default('0.2.32') }#
  url: oci://ghcr.io/jrmatherly/charts/obot
#% endif %#
