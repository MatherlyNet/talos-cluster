#% if cnpg_backup_enabled | default(false) %#
---
#| CNPG backup credentials for S3-compatible storage (RustFS) #|
#| These credentials are used by Barman Cloud plugin for PostgreSQL backups #|
#| Create access keys via RustFS Console UI - see docs/guides/cnpg-implementation.md #|
apiVersion: v1
kind: Secret
metadata:
  name: cnpg-backup-credentials
type: Opaque
stringData:
  ACCESS_KEY_ID: "#{ cnpg_s3_access_key }#"
  SECRET_ACCESS_KEY: "#{ cnpg_s3_secret_key }#"
#% endif %#
