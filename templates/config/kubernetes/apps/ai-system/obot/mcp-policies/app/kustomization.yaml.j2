#% if obot_enabled | default(false) %#
---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  #| RBAC for Obot to manage MCP server pods #|
  - ./rbac.yaml
  #| ResourceQuota and LimitRange for MCP servers #|
  - ./resourcequota.yaml
#% if network_policies_enabled | default(false) %#
  #| Network isolation for dynamically spawned MCP servers #|
  - ./networkpolicy.yaml
#% endif %#
#% endif %#
