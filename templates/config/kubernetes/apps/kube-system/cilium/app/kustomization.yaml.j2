---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./helmrelease.yaml
  - ./ocirepository.yaml
  - ./networks.yaml
#% if cilium_bgp_password is defined %#
  - ./secret.sops.yaml
#% endif %#
#| NOTE: Hubble UI HTTPRoute moved to network/envoy-gateway for OIDC protection #|
#% if hubble_enabled | default(false) %#
  #| ReferenceGrant must be in kube-system (target namespace) for cross-ns routing #|
  - ./referencegrant.yaml
#% endif %#
