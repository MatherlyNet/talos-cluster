#% if keycloak_enabled | default(false) %#
---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  #| Plain ConfigMap with $(env:VAR) placeholders for secrets #|
  - ./realm-config.yaml
  #| SOPS-encrypted Secret with actual secret values #|
  - ./secrets.sops.yaml
  - ./config-job.yaml
#% if network_policies_enabled | default(false) %#
  - ./networkpolicy.yaml
#% endif %#
#% endif %#
